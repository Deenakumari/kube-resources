apiVersion: v1
kind: Service
metadata:
   name: Service
   labels:
     purpose: service-demo
spec:
   selector:
     type: demo
     purpose: service-demo
     tier: frontend
   ports:
     - protocol: TCP
       port: 80
       targetPort: 80  
     

---
apiVersion: v1
kind: Pod
metadata: 
   name: nginx
   labels:
     type: demo
     purpose: service-demo
     tier: frontend
spec:
   containers:
    - name: nginx
      image: nginx       

